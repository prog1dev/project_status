require 'spec_helper'

describe ProjectStatus do
  it 'has a version number' do
    expect(ProjectStatus::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
